services:
  worker:
    build: ./worker
    restart: always
    env_file: .env
    volumes:
      - worker_tmp:/app/tmp
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  worker_tmp:
